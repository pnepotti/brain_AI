# This file is used by Alembic to auto-generate migrations.
# It needs to import the Base and all models that should be managed.

from sqlalchemy.orm import declarative_base

Base = declarative_base()